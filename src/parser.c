// UNFINISHED

struct ParsedString {
    char* command;
};

char* parseString(const char* source) {
    char* command;
    char* tmp;

    for (int i = 0; i < strlen(source); i++) {
        if (source[i] == ' ') {
            
        }
    }
}